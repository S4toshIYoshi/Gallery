import icon from '../images/vectorDown.svg';

const VectorDown = () => {
	return (
		<img
			src={icon}
			alt='vector down'
			style={{cursor: 'pointer', zIndex: '3'}}
		/>
	);
};

export default VectorDown;
